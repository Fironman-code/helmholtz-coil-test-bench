[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "helmholtz-coil-test-bench"
version = "0.1.0"
description = "Tools for Helmholtz coil calculations and measurement analysis."
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]